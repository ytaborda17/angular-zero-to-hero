import { ContactPipePipe } from './contact-pipe.pipe';

describe('ContactPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactPipePipe();
    expect(pipe).toBeTruthy();
  });
});
